**React reacts to [[state]] changes (data) by [[Re-render]] the [[component]].**  
When the state of a component changes, React triggers a **re-render** process.  
During this process, it compares the new [[Virtual DOM]] with the previous one and efficiently updates only the parts of the [[UI]] that actually changed.