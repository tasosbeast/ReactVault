What do we do in the Front-End?

We keep the [[User Interface (UI)|UI]] **in sync** with the available data (usually the [[state]]), so it reacts immediately and correctly to changes.  
When the data changes, React triggers a new [[render]], and the [[component]]s update accordingly.

[[Re-render]]