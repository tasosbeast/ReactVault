**Data = [[State]]**

In React, most of the **data** that determines what the [[UI]] looks like is stored in **[[state]]**.  
When the **state changes**, React automatically triggers a new [[render]] so the UI stays in sync with that data. [[Re-render]]