**In Vanilla JavaScript, we manually select and update each element in the [[DOM]].**  
In contrast, React uses [[JSX]] to **abstract** that process — it automatically handles the **creation**, **update**, and **reconciliation** of DOM elements through the [[Virtual DOM]].

This means you describe _what_ the UI should look like, and React takes care of _how_ to make the real DOM match it.