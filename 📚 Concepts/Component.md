
A function or class that returns **[[JSX]]** (what gets rendered to the screen).



**Components are the building blocks of the [[User Interface (UI)|UI]] in React.**  
Each component represents a **reusable, independent piece** of the interface — like a button, form, or list — that defines **how it looks** and **how it behaves** based on its [[props]] and [[state]].